{
  "name": "dukaanos",
  "version": "0.1.0",
  "private": true,
  "description": "DukaanOS — Your business finally remembers. AI-powered persistent business memory for local Indian businesses.",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "test": "turbo run test",
    "test:all": "turbo run test test:e2e test:security test:eval test:a11y",
    "lint": "turbo run lint",
    "typecheck": "turbo run typecheck",
    "clean": "turbo run clean",
    "deploy:staging": "cd packages/infra && npx cdk deploy --all --require-approval never",
    "deploy:production": "cd packages/infra && npx cdk deploy --all --require-approval broadening",
    "seed": "npx tsx scripts/seed-demo.ts",
    "smoke-test": "bash scripts/smoke-test.sh",
    "teardown": "bash scripts/teardown.sh"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "prettier": "^3.4.0",
    "turbo": "^2.3.0",
    "typescript": "^5.6.0"
  },
  "engines": {
    "node": ">=20.0.0"
  },
  "packageManager": "npm@10.0.0",
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.1135.0",
    "@aws-sdk/lib-dynamodb": "^3.1135.0"
  }
}
